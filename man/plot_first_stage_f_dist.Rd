% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/visualization.R
\name{plot_first_stage_f_dist}
\alias{plot_first_stage_f_dist}
\title{Create First-Stage F Distribution Plot}
\usage{
plot_first_stage_f_dist(results_clean, config = NULL, weak_iv_pct = NULL)
}
\arguments{
\item{results_clean}{Data.frame. Cleaned simulation results.}

\item{config}{List. Optional. Configuration object (not currently used).}

\item{weak_iv_pct}{Numeric. Percentage of simulations with weak instruments.}
}
\value{
A ggplot2 object.
}
\description{
Creates a histogram of first-stage F-statistics with weak instrument
threshold.
}
\examples{
\dontrun{
config <- create_default_config()
seeds <- generate_all_seeds(config)
results <- run_main_simulation(config, seeds)
results_clean <- na.omit(results)
weak_iv_pct <- mean(results_clean$first_stage_F < 10) * 100
p4 <- plot_first_stage_f_dist(results_clean, weak_iv_pct)
print(p4)
}

}
